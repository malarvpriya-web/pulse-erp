# This file is retired — it was a stale, out-of-sync copy of the real Render
# Blueprint (older `plan: free`, `npm install` instead of `npm ci`, and a
# pre-manifest-tech.in FRONTEND_URL). Fixing all three drifted from here.
#
# The canonical, actually-deployed manifest is:
#   Pulse/render.yaml
#
# Edit that file only. This one is kept solely so nothing 404s if it's bookmarked.
